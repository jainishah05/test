{
    "fields": [{
            "name": "title",
            "type": "string"
        },
        {
            "name": "link",
            "type": "string"
        },
        {
            "name": "image",
            "type": "file"
        },
        {
            "name": "status",
            "type": "boolean"
        }
    ],

    "relationships": [{
        "name": "user",
        "type": "belongsTo",
        "class": "App\\User"
    }],

    "validations": [{
        "field": "link",
        "rules": "required"
    }]
}